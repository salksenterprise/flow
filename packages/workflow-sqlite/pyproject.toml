[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "flow-workflow-sqlite"
version = "0.3.0"
description = "SQLite persistence adapter for Flow"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["src"]
