[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "flow-workflow-core"
version = "0.2.0"
description = "Domain-neutral graph and FSM workflow execution core"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["src"]

